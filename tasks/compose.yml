---
- name: Render docker-compose.yml
  ansible.builtin.template:
    src: docker-compose.yml.j2
    dest: "{{ redash_compose_file }}"
    owner: root
    group: root
    mode: "0640"
    validate: "docker compose -f %s config -q"
  register: _compose_changed
  notify: redeploy redash stack
